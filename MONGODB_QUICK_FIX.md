# ⚡ MONGODB CONEXIÓN - PASOS INMEDIATOS\n\n## 🔴 PROBLEMA ACTUAL\n\n```\nMongoNetworkError: SSL routines:ssl3_read_bytes:tlsv1 alert internal error\n```\n\n**Significa**: MongoDB Atlas rechaza la conexión SSL\n\n---\n\n## ✅ SOLUCIONES RÁPIDAS (En orden)\n\n### PASO 1: Obtén tu IP Pública (2 minutos)\n\n```powershell\n# En PowerShell, ejecuta:\n(Invoke-WebRequest -Uri 'https://api.ipify.org?format=json').Content | ConvertFrom-Json | ForEach-Object { Write-Host \"Tu IP: $($_.ip)\" }\n```\n\n**Copia la IP que sale**\n\n---\n\n### PASO 2: Agrega IP a MongoDB Atlas (2 minutos)\n\n1. Ve a: https://www.mongodb.com/cloud/atlas\n2. Login con tu cuenta\n3. Click en **\"Network Access\"** (lado izquierdo)\n4. Click en **\"Add IP Address\"**\n5. Pega tu IP en el campo\n6. Click **\"Confirm\"**\n7. **ESPERA 1-2 MINUTOS**\n\n---\n\n### PASO 3: Obtén Connection String Correcto (2 minutos)\n\n1. En MongoDB Atlas, click en **\"Connect\"**\n2. Selecciona **\"Drivers\"**\n3. Elige **\"Node.js\"**\n4. **COPIA** la connection string (comienza con `mongodb+srv://`)\n5. En ese string, **reemplaza**:\n   - `<password>` → tu contraseña\n   - `<database>` → `greentech-hub`\n\n**Debe verse como**:\n```\nmongodb+srv://usuario:miContraseña123@tucluster.mongodb.net/greentech-hub?retryWrites=true&w=majority\n```\n\n---\n\n### PASO 4: Actualiza .env (1 minuto)\n\nAbre `backend/.env` y asegúrate que tenga:\n\n```\nMONGODB_URI=mongodb+srv://usuario:miContraseña123@tucluster.mongodb.net/greentech-hub?retryWrites=true&w=majority\nSKIP_DB=false\n```\n\n**IMPORTANTE**: Reemplaza con TUS valores de MongoDB Atlas\n\n---\n\n### PASO 5: Reinicia Backend (1 minuto)\n\n```bash\ncd backend\nnpm start\n```\n\n**Esperado**:\n```\nConnected to MongoDB ✅\nServer running on port 3000 ✅\n```\n\n---\n\n## ⚠️ SI AÚN NO FUNCIONA\n\n### Plan B: Usa MongoDB Local\n\n```bash\n# 1. Descarga MongoDB Community\n#    https://www.mongodb.com/try/download/community\n#    Instala normalmente\n\n# 2. Actualiza .env a:\necho \"MONGODB_URI=mongodb://localhost:27017/greentech-hub\" > backend/.env\necho \"SKIP_DB=false\" >> backend/.env\n\n# 3. Reinicia backend\ncd backend\nnpm start\n```\n\n---\n\n## 🧪 TEST DE CONEXIÓN\n\nSi quieres verificar que la conexión funciona:\n\n```bash\n# Crea este archivo: backend/test-connection.js\n```\n\n```javascript\nrequire('dotenv').config();\nconst mongoose = require('mongoose');\n\nconst uri = process.env.MONGODB_URI || 'mongodb://localhost:27017/greentech-hub';\nconsole.log('Conectando a:', uri.substring(0, 50) + '...');\n\nmongoose.connect(uri, {\n  useNewUrlParser: true,\n  useUnifiedTopology: true,\n  tls: true,\n  tlsAllowInvalidCertificates: true,\n}).then(() => {\n  console.log('✅ CONEXIÓN EXITOSA');\n  process.exit(0);\n}).catch(err => {\n  console.error('❌ ERROR:', err.message);\n  process.exit(1);\n});\n```\n\nEjecuta:\n```bash\nnode backend/test-connection.js\n```\n\n---\n\n## ✅ CHECKLIST\n\n- [ ] Obtuve mi IP pública\n- [ ] Agregué IP a MongoDB Atlas Whitelist\n- [ ] Esperé 1-2 minutos\n- [ ] Obtuve connection string correcto\n- [ ] Actualicé backend/.env\n- [ ] Reinicié backend con npm start\n- [ ] Veo \"Connected to MongoDB\" ✅\n\n---\n\n**Tiempo estimado**: 10-15 minutos  \n**Status**: 🔄 BLOQUEADO EN MONGODB - SIGUE ESTOS PASOS\n\n